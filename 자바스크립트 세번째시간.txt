
변수 - 단일 값
객체 - 복수 값
일반객체 -복수 값/ 텍스트 & 메서드
변칙적인 객체  - 배열/ 문자열객체 
 

함수 
function 함수명 (매개변수 (*선택적 = > 인수 혹은 인자값)) {

함수 실행문 

}

함수를 선언하면 함수를 호출을해야한다.
마지막에 함수명을 입력해야한다.




// 전역 변수 % 지역 변수 & 블록 변수 




변수는 함수도 담을 수 있다.
변수에 함수를 담으면 함수가 된다.

let sum = function () -> 함수에 이름이 없으면 익명함수

querySelector - > 매개 변수에 선택적으로 가져온다는 함수

btn.addEventListener -> 앞에 매개 변수를 실행해주는 것

btn.addEventListener("click", display); 함수 안에 함수가 들어가는것은 콜백함수 CB함수 


1. 변수에 값을 할당 할 수 있어야 1급 시민

2. 다른 함수의 매개변수 혹은 인자값으로 사용할 수 있어야 1급 시민

3. 다른 함수에서 반환값으로 반환할 수 있어야 한다!



console.log(...fruits); 배열에서 골라오고 싶은때 쓴다.


for of 문은 배열안에 어떤 값을 반복하고 싶을때 사용


0은 false로 의미 

String() 문자형
숫자 => Number()/parselnt()/parseFloat()
논리 => Boolean
